CREATE TABLE report (report_id NUMBER NOT NULL PRIMARY KEY, film_id NUMBER, film_name VARCHAR,
                     character_id NUMBER, character_name VARCHAR,
                     planet_id NUMBER, planet_name VARCHAR);